spring.application.name=mtitek-spring-oauth-resource-server

server.port=8081

#the JWK set URL will be used to fetch the public key from the authorization server to verify the token's signature
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://mtitekauthserver:8082/oauth2/jwks